name: Pallet Tests
on:
  push:
    branches: [ add-pallet-auto-tasks ]
  pull_request:
    branches: [ State-Driven-Task ]
env:
  CARGO_TERM_COLOR: always
jobs:
  test:
    name: Test Pallet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          target: wasm32-unknown-unknown
          components: rustfmt, clippy
          override: true
          profile: minimal
      
      - name: Cache Dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          
      - name: Fix Format
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo fmt
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Fix formatting" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}
          git push origin HEAD:${{ github.ref }} || echo "No changes to push"
          
      - name: Check Format
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo fmt -- --check
          
      - name: Clippy Check
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo clippy -- -D warnings
          
      - name: Run Tests
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo test
          
      - name: Run Tests with Experimental Feature
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo test --features experimental
          
      - name: Build Documentation
        run: |
          cd substrate/frame/pallet-auto-tasks
          cargo doc --no-deps